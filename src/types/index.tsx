export type TRoomData = {
  id: number;

  title: string;
  location: string;
  noOfBath: number;
  noOfBed: number;
};

export type TTabCount = {
  title: string;
  count: number;
};

export type TTopDeveloper = {
  id: number;
  name: string;
  description: string;
};

export type TTestimonial = {
  id: number;
  name: string;
  testimonial: string;
};
